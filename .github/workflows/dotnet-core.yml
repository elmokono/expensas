on:
  push:
    branches: [ master ]

jobs:
  deploy_job:
    runs-on: ubuntu-latest
    name: deploy
    steps:
      - name: checkout
        uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - name: deploy file
        uses:  swznd/sftp-deploy@master
        with:
          host: 34.105.44.99
          user: elmokono
          private_key: ${{ secrets.SSH_PRIVATE_KEY }}
          remote_path: /home/elmokono/www/publish/expensas
          ignore: .github/**